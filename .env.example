# ─── PostgreSQL ────────────────────────────────────────────────────────────
# Shared with other services; in cluster injected via Vault (ms-secret-vault).
POSTGRES_USER=ceramicraft
POSTGRES_PASSWORD=ceramicraft123
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
LOG_DB_NAME=log_db

# ─── HTTP Health-check Server ─────────────────────────────────────────────
LOG_MSERVICE_HTTP_HOST=0.0.0.0
LOG_MSERVICE_HTTP_PORT=8080

# ─── gRPC Server ──────────────────────────────────────────────────────────
LOG_MSERVICE_GRPC_HOST=[::]
LOG_MSERVICE_GRPC_PORT=50051
